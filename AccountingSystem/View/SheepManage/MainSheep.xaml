<UserControl x:Class="AccountingSystem.View.SheepManage.MainSheep"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:AccountingSystem.View.SheepManage"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="WhiteSmoke"
          FlowDirection="RightToLeft">
        <Grid.RowDefinitions>
            <RowDefinition Height="40" />
            <RowDefinition />
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal"
                    Grid.Row="0"
                    Name="GridNameButtons"
                    Margin="5 0">

            <Button  Width="140"
                     Uid="0"
                     Height="40"
                     Content="فواتير شراء"
                     Background="{x:Null}"
                     BorderBrush="{x:Null}"
                     Foreground="{StaticResource MainTextColor}"
                     Click="btnMainButtons" />
            <Button  MinWidth="140"
                     Uid="1"
                     Height="40"
                     Content="فواتير مبيعات"
                     Background="{x:Null}"
                     BorderBrush="{x:Null}"
                     Foreground="{StaticResource MainTextColor}"
                     Click="btnMainButtons" />

        </StackPanel>

        <Border Background="{StaticResource MainColor}"
                Padding="5"
                CornerRadius="15"
                Margin="5"
                Grid.Row="2"
                BorderThickness="2"
                BorderBrush="DodgerBlue">
            <Grid x:Name="GridMain"
                  Background="{StaticResource MainColor}">

            </Grid>
        </Border>


    </Grid>

</UserControl>
